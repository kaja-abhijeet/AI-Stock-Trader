# 🤖 Q-Learning Stock Trading Bot

A simple **Reinforcement Learning** project that uses **Q-Learning** to trade real stock data from **Yahoo Finance**.  
The bot learns when to **buy**, **sell**, or **hold**, and visualizes its performance using a sleek **Streamlit dashboard**.

🧠 Built with: `Python`, `Streamlit`, `NumPy`, `Pandas`, `Matplotlib`, `yfinance`

🎯 Features:
- Real stock data (AAPL, TSLA, etc.)
- Interactive Streamlit UI
- Buy/Sell action visualization
- Portfolio vs Buy & Hold comparison
- Reward tracking across episodes

🚀 Run it locally:
```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
